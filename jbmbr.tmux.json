[
  {
    "left": "cd ~; tmux rename-window -t 1 'shell'; brew update; npm-check-updates -g; brew outdated"
  }
  , {
    "top": {
      "left": "z tussle && nodemon"
      , "right": "z tussle; tmux rename-window -t 2 'tussle'; tmux select-layout -t 2 even-horizontal"
    }
    , "bottom": "z uwn-artiist && nodemon -x mocha -- test"
  }
  , {
    "left": "z staticblog; tmux rename-window -t 3 'blog'"
    , "right": "z staticblog && grunt code"
  }
  , {
    "left": "z wheelhouse-router; tmux rename-window -t 3 'router'"
    , "right": "z wheelhouse-router && nodemon -x grunt -i test/fixtures/main.compiled.js -- test"
  }
  , {
    "left": "z wheelhouse-resource; tmux rename-window -t 4 'resource'"
    , "right": "z wheelhouse-resource && npm run dev"
  }
]
